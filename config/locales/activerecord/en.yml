en:
  errors:
    format: "%{message}"
  activerecord:
    errors:
      models:
        schedule_rule:
          attributes:
            position:
              required: "A position must be selected"
            period:
              blank: "A period must be selected"
              taken: "A schedule rule already exists for this position and
                constraint"
