<footer class="centered">
  <% if 1 == 2 %>
  <% if presenter.scheduling_period.present? %>
    <%= link_to(
      "Add Shift",
      new_remote_new_calendar_scheduling_period_in_progress_shift_path(
        presenter.scheduling_period,
        date: presenter.date_integer
      ),
      class: "button",
      remote: true
    ) %>
  <% end %>
<% end %>
</footer>
