<div class="frame">
  <header>
    <div>
      <%= t(".title") %>

      <% if policy(User).new? %>
        <aside>
          <%= link_to t(".add_employee"), new_business_user_path %>
        </aside>
      <% end %>
    </div>
  </header>


  <section>
    <% if @presenter.users.empty? %>
      <article class="centered">
        <%= t(".no_users") %>
      </article>
    <% else %>
      <%= render partial: "users/users", locals: { presenter: @presenter } %>
    <% end %>
  </section>
</div>
