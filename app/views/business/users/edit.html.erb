<div class="frame">
  <section>
    <%= simple_form_for [:business, @user] do |f| %>
      <%= f.input :email %>
      <%= f.association :positions,
        collection: current_company.positions,
        as: :check_boxes %>

      <%= f.submit %>
    <% end %>
  </section>
</div>
