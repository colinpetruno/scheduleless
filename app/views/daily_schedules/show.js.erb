$(".selected-day").removeClass("selected-day");
$(".<%= @presenter.date.to_s %>").addClass("selected-day");

$("#daily-schedule").
  html("<%= j(render partial: "calendars/day_schedule", locals: {
    presenter: @presenter
  }) %>");

$("#daily-schedule").
  find('[data-toggle="tooltip"]').
  tooltip();
