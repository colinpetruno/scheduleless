<div class="list-nav">
  <ul>
    <%= content_tag(:li, class: active?(edit_location_path(@location))) do %>
      <%= link_to "Details", edit_location_path(@location) %>
    <% end %>

    <%= content_tag(:li, class: active?(locations_location_users_path(@location))) do %>
      <%= link_to "Employees", locations_location_users_path(@location) %>
    <% end %>
  </ul>
</div>
