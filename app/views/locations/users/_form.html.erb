<%= simple_form_for [:locations, @location, @user] do |f| %>
  <%= f.input :email %>
  <%= f.input :mobile_phone %>
  <%= f.input :given_name %>
  <%= f.input :family_name %>

  <%= f.submit %>
<% end %>
