<div class="frame">
  <header>
    <div>
      <%= t(".title") %>
      <aside>
        <%= link_to_if policy(:location).new?, t(".new_location"), new_location_path %>
      </aside>
    </div>
  </header>

  <section>
    <table>
      <thead>
        <tr>
          <th></th>
          <th></th>
          <th></th>
          <tr></th>
        </tr>
    <% @locations.each do |location| %>
      <tr>
        <td>
          <%= link_to_if policy(location).edit?, t(".edit"), edit_location_path(location) do %>
            <%= link_to t(".show"), location_path(location) %>
          <% end %>
        </td>
        <td><%= location.line_1 %></td>
        <td><%= location.city %></td>
        <td><%= location.county_province %></td>
      </tr>
    <% end %>
  </section>
</div>
