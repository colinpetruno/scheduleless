<div class="month-headers"> <!-- row -->
  <% Date::ABBR_MONTHNAMES.compact.map do |month| %>
    <div class="<%= "selected" if month.downcase == "feb" %>">
      <%= month %>
    </div>
  <% end %>
</div>
