<%= t(".title") %>

<%= @location.name if @location.name.present? %>
<%= @location.line_1 if @location.line_1.present? %>
<%= @location.line_2 if @location.line_2.present? %>
<%= AddressFormatter.for(@location).city_state_zip %>

<%= t(".message", dates: @scheduling_period.label) %>

<%= t(".link") %>:
<%= location_calendar_url(@location) %>
