$(".view-sidebar > .popup > .content").html("<%= j(
  render partial: "form", locals: { shift: @shift }
) %>");

window.Scheduleless.instantiatePickers();
$(".view-sidebar > .popup").show().addClass("open");
