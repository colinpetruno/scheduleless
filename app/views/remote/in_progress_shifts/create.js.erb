<% if @shift.errors.present? %>
    <%# TODO: ADD BETTER ERROR MESSAGING %>
    console.log("errors");
<% else %>
  $(".popup").removeClass("open");
  $("#new-calendar-show-calendar").html("<%= j(render(partial: "new_calendars/show",
               locals: {
                 presenter: NewCalendar::ShowPresenter.
                   new(date: @shift.date,
                       location: @location,
                       user: current_user,
                       view: :weekly
                      )
  })) %>");
<% end %>
