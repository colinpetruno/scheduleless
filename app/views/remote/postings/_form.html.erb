<div class="remote-postings-form">
  <%= simple_form_for [:remote, @location, posting], remote: true do |f| %>

    <%= f.input :all_shifts,
      as: :boolean,
      label: "Publish all outstanding shifts",
      input_html: {
        class: "disable-toggle",
        data: { target: ".posting-date-range" }
      } %>

    <div class="posting-date-range">
      <%= f.input :date_start, as: :datepicker_range %>
    </div>

    <br />

    <%= f.submit %>
  <% end %>

  <br /><br/>
  <div class="centered">
    A posting publishes any shift changes you created to your
    employees. You can publish all outstanding shifts or pick a date range.
    If you want to publish a single shift, simply edit it and publish.
  </div>
</div>
