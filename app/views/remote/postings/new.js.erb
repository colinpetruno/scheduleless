$(".view-sidebar > .popup > .content").html("<%= j(
  render partial: "form", locals: { location: @location, posting: @posting }
) %>");

window.Scheduleless.reinstantiatePickers();

$(".view-sidebar > .popup").show().addClass("open");
