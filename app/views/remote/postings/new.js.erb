window.Scheduleless.sidebar.appendAndOpen("<%= j(
  render partial: "form", locals: {
    location: @location,
    posting: @posting,
    display_date: params[:display_date] || Date.today
  }
) %>");

window.Scheduleless.instantiateDisableToggles();
