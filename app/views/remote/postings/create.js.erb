window.Scheduleless.sidebar.close();

$("#calendar-container").html("<%= j(render(partial: "calendars/show",
             locals: {
               presenter: presenter_class.
                 new(date: @posting.date_start,
                     location: @location,
                     user: current_user
                    )
})) %>");
