window.Scheduleless.sidebar.close();

window.Scheduleless.calendar.redraw("<%= j(render(partial: "calendars/show",
             locals: {
               presenter: presenter_class.
                 new(date: @posting.date_start,
                     location: @location,
                     user: current_user
                    )
})) %>");
