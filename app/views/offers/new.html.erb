<%= simple_form_for [@offer.trade, @offer] do |f| %>
  <%= f.input :note %>

  <%= f.input :offered_trade_id, as: :select, collection: ShiftFinder.for(current_user).future %>

  <%= f.submit %>
<% end %>
